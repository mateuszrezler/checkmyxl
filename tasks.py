from custom import contains_digit, is_bool, is_greatest_in_row, is_unique, \
    show_groups

TASKS = {
    0: contains_digit,
    1: is_bool,
    2: is_greatest_in_row,
    3: show_groups,
    4: is_unique
}
